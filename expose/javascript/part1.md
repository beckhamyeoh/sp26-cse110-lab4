1. values added: 20
2. values added: 20
3. in this example, var ignored the if block. this is unpredictable behavior and can cause bugs. if someone accidentally uses the same name for a different variable, the program may not run as intended. let and const are alternatives that should be used instead of var because there is more control. 
4. values added: 20
5. error because result is a block-scoped variable declared in the if block and cannot be used outside of it
6. error because result is a const and its value cannot be changed. this is a TypeError
7. error because result is a block-scoped variable declared in the if block and cannot be used outside of it
